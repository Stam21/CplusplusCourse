#include <iostream>

using namespace std;

int main() {
    double x, y, z;
    
    // Read the three double numbers
    cin >> x >> y >> z;

    // Calculate the result of the expression
    double result = (x + y) - z;

    // Print the result
    cout <<  result << endl;

    return 0;
}